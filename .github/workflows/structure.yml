name: Structure & Naming

on:
  pull_request:
  push:
    branches: [main]

jobs:
  check-structure:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Check repo structure and naming conventions
        run: python3 scripts/ci/check_structure.py
